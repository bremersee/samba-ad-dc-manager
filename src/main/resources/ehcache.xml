<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns="http://www.ehcache.org/v3"
  xmlns:jsr107="http://www.ehcache.org/v3/jsr107"
  xsi:schemaLocation="
    http://www.ehcache.org/v3 http://www.ehcache.org/schema/ehcache-core-3.0.xsd
    http://www.ehcache.org/v3/jsr107 http://www.ehcache.org/schema/ehcache-107-ext-3.0.xsd">

  <!--
  <service>
    <jsr107:defaults enable-management="true" enable-statistics="true" default-template="default" />
  </service>
  -->

  <!-- TODO -->
  <!-- <persistence directory="${java.io.tmpdir}"/> -->
  <persistence directory="/Users/cbr/cache/ad-dc-manager"/>

  <cache-template name="default">
    <expiry>
      <ttl unit="hours">1</ttl>
    </expiry>
    <listeners>
      <listener>
        <class>org.bremersee.samba.ad.dc.config.CacheEventLogger</class>
        <event-firing-mode>ASYNCHRONOUS</event-firing-mode>
        <event-ordering-mode>UNORDERED</event-ordering-mode>
        <events-to-fire-on>EVICTED</events-to-fire-on>
        <events-to-fire-on>EXPIRED</events-to-fire-on>
        <events-to-fire-on>REMOVED</events-to-fire-on>
        <events-to-fire-on>CREATED</events-to-fire-on>
        <events-to-fire-on>UPDATED</events-to-fire-on>
      </listener>
    </listeners>
    <resources>
      <heap>1000</heap>
      <offheap unit="MB">4</offheap>
      <disk persistent="true" unit="MB">8</disk>
    </resources>
  </cache-template>

  <cache alias="dnsZoneCache" uses-template="default">
    <value-type>org.bremersee.samba.ad.dc.dns.model.DnsZone</value-type>
  </cache>

  <cache alias="dnsZoneListCache" uses-template="default">
  </cache>

  <cache alias="dnsEntryListCache" uses-template="default">
  </cache>

  <cache alias="dhcpLeasesCache" uses-template="default">
  </cache>

  <!--
  <cache alias="dhcp-leases-by-name" uses-template="dhcp-leases-template">
  </cache>

  <cache alias="password-information">
    <expiry>
      <ttl unit="minutes">1</ttl>
    </expiry>
    <resources>
      <heap unit="entries">1</heap>
      <offheap unit="MB">1</offheap>
    </resources>
  </cache>

  <cache alias="jwt">
    <expiry>
      <ttl unit="minutes">15</ttl>
    </expiry>
    <resources>
      <heap unit="entries">10</heap>
      <offheap unit="MB">1</offheap>
    </resources>
  </cache>
  -->

</config>